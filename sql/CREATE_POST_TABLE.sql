CREATE TABLE POST
(
    ID          INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    MESSAGE     TEXT                           NOT NULL,
    DATE_POSTED TIMESTAMP DEFAULT CURRENT_DATE NOT NULL,
    USER_POSTED INT REFERENCES USERS           NOT NULL
);